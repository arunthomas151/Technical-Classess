@model IEnumerable<Student.Models.Stud>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dept.dname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Registration.sname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dept.dname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Registration.sname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudId })
        </td>
    </tr>
}

</table>
